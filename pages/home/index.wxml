<!--index.wxml-->
<navigation-bar title="{{appletName}}" back="{{false}}" color="#3f3d3d" background="#FFF"></navigation-bar>
<scroll-view class="scrollarea" scroll-y type="list">
	<view class="all">
	    <!--轮播图  -->
	    <view class='nav'>
	        <swiper class="swiper" indicator-dots="true" autoplay="true" current="{{current}}" interval="5000" duration="500"   circular="true">
	            <!-- <block wx:for="{{lun_ad}}" wx:key="item">
	                <navigator url='../detail/detail?workId={{item.id}}'>
	                    <swiper-item>
	                        <image src='{{blogUrl}}/resource/images/lunbo/1.jpg' class='img' mode=""></image>
	                    </swiper-item>
	                </navigator>
	            </block> -->
	            <navigator url='../detail/detail?workId={{item.id}}'>
                    <swiper-item class="lunbo">
·                        <image src='{{blogUrl}}/resource/images/lunbo/1.jpg' class='img' mode=""></image>
                    </swiper-item>
                </navigator> 
	            <navigator url='../detail/detail?workId={{item.id}}'>
                    <swiper-item class="lunbo">
                        <image src='{{blogUrl}}/resource/images/lunbo/2.jpg' class='img' mode="" ></image>
                    </swiper-item>
                </navigator> 
	            <navigator url='../detail/detail?workId={{item.id}}'>
                    <swiper-item class="lunbo">
                        <image src='{{blogUrl}}/resource/images/lunbo/3.jpg' class='img' mode=""></image>
                    </swiper-item>
                </navigator> 
	            <navigator url='../detail/detail?workId={{item.id}}'>
                    <swiper-item class="lunbo">
                        <image src='{{blogUrl}}/resource/images/lunbo/4.jpg' class='img' mode=""></image>
                    </swiper-item>
                </navigator>  
	        </swiper>
	    </view>
	    <!-- 博客 文章 -->
	    <view class='works'>
			<block  wx:for="{{works}}" wx:key="item">
	            <navigator url='../detail/detail?workId={{item.id}}'>
	                <view class="work-all">
	    				<image src='{{blogUrl}}/{{item.img}}' class="work-img" binderror="workImgloadError" mode="aspectFill" data-errorimg="{{index}}"></image>
	    				<view class="work-desc">
	    					<view class="work-title">{{item.title}}</view>
	    					<view class="work-cont">{{item.desc}}</view>
	    					<view class="work-sign">{{item.browse_num}}阅读量</view>
	    				</view>
	                </view>
	            </navigator>
			</block>
	    </view>
	</view>
</scroll-view>
